import React from 'react';
import {

    Button
} from '@mui/material';
import DogCard from './dogCard';

const HomeScreen = ( props ) => {
    return (
        <React.Fragment>
            <DogCard/>
        </React.Fragment>
    )
}

export default HomeScreen;